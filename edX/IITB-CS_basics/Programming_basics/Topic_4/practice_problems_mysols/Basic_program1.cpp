#include<iostream>
using namespace std;
int main(){
	int l,w,area,perim;
	cout<<"Enter the length of the rectangle: ";cin>>l;
	cout<<"Enter the width of the rectangle: ";cin>>w;
	cout<<"The area of the reactangle is (l x w) = "<<l*w<<endl;
	cout<<"The perimeter of the rectangle is 2(l + w) = "<<2*(l+w)<<endl;
	return 0;
}
